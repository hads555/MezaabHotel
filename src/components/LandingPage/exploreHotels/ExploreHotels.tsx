import React from 'react';
import styles from './ExploreHotels.module.css';

const ExploreHotels: React.FC = () => {
  return (
    <div className={`${styles.mainWrapper}`}>

    </div>
  )
}

export default ExploreHotels